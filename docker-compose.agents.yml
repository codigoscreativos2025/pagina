version: '3.8'

services:
  # Frontend App - agents.pivotsoluciones.com
  frontend-app:
    build:
      context: ./pivots/frontend-app
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=/api
    restart: unless-stopped
