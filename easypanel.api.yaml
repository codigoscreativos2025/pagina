# EasyPanel Configuration - API Backend
# Puerto: 3001
name: agents-api
logo: ""

# Build con Dockerfile desde api/
build:
  docker:
    file: api/Dockerfile

# Puerto donde corre la API
port: 3001

# Solo proxy, sin SSL externo (interno)
features:
  - proxy
