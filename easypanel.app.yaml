# EasyPanel Configuration - Agents App
# Puerto: 3000
name: agents
logo: ""

# Build con Dockerfile desde app/
build:
  docker:
    file: app/Dockerfile

# Puerto donde corre la app
port: 3000

features:
  - proxy
  - ssl

# Variables de entorno
env:
  - key: VITE_API_URL
    value: /api
